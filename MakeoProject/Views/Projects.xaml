<UserControl x:Class="MakeoProject.Views.Projects"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MakeoProject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="1" Content="Projets" HorizontalAlignment="Center"/>
        <Button Grid.Row="2" Content="Ajouter" HorizontalAlignment="Center"/>
        <Button Grid.Row="3" Content="Supprimer" Click="RemoveButton_Click" HorizontalAlignment="Center"/>
        <DataGrid x:Name="dgProject" Grid.Row="4" AutoGenerateColumns="False" ItemsSource="{Binding allproject}" SelectedItem="{Binding SelectedProject}" CanUserReorderColumns="False" CanUserResizeColumns="False" />
    </Grid>
</UserControl>
